# Facebook/Instagram MCP Action Skill Configuration
name: facebook_mcp_action
version: "1.0.0"
enabled: true

config:
  # MCP server URL
  mcp_url: http://localhost:8006

  # Request timeout (seconds)
  timeout: 30

  # Default post settings
  default_platform: facebook

  # Approval settings
  require_approval_for_all_posts: true

# Vault path
vault_path: D:\prompteng\AI_Employee_Vault
